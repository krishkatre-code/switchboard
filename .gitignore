# dependencies
node_modules/

# build output
dist/

# secrets & local config
.env
.env.*
!.env.example
*-sa.json
service-account*.json

# local data store (feedback / overrides / recent)
data/

# internal source of truth — NOT for publishing to this repo
reference/

# logs / os
*.log
.DS_Store
